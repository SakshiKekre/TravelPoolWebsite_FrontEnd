version: 0.2


phases:
 pre_build:
  commands:
   - npm install
 build:
  commands:
   - npm run build
 post_build:
  commands:
   - aws s3 cp --recursive ./build http://hostbucket282.s3-website-us-east-1.amazonaws.com
